// Re-exports only what this module still consumes; import the rest from
// `@openrift/shared/db-field-rules` directly.
export {
  candidateCardFieldRules,
  candidatePrintingFieldRules,
} from "@openrift/shared/db-field-rules";
