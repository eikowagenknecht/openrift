{
  "name": "openrift",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "check:asset-types": "bun scripts/check-asset-types.ts",
    "check:override-pins": "bun scripts/check-override-pins.ts",
    "typecheck": "turbo run typecheck",
    "dev": "DEV_HTTPS=1 bun scripts/dev.ts",
    "dev:http": "bun scripts/dev.ts",
    "dev:screenshot": "VITE_DISABLE_DEVTOOLS=1 bun scripts/dev.ts",
    "dev:web": "turbo run dev --filter=web",
    "analyze": "bun run --cwd apps/web analyze",
    "format": "oxfmt",
    "lint": "bun run lint:oxlint && bun run lint:eslint && bun run lint:oxfmt",
    "lint:oxlint": "oxlint --deny-warnings",
    "lint:eslint": "bun run --cwd apps/web lint",
    "lint:oxfmt": "oxfmt ./apps ./packages --check",
    "lefthook": "lefthook",
    "db:migrate": "bun run --cwd packages/shared db:migrate",
    "db:schema": "bun --env-file=.env scripts/db-schema.ts",
    "db:cleanup": "bun --env-file=.env scripts/db-cleanup.ts",
    "db:dev-passwords": "bun --env-file=.env scripts/reset-dev-passwords.ts",
    "db:rollback": "bun run --cwd packages/shared db:rollback",
    "make-admin": "bun run --cwd packages/shared make-admin --",
    "dev:api": "bun --env-file=.env --watch apps/api/src/index.ts | bunx pino-pretty",
    "discord": "bun run --cwd apps/discord-bot dev:prod",
    "discord:emojis": "bun --env-file=.env scripts/upload-discord-emojis.ts",
    "routes:generate": "bun run --cwd apps/web routes:generate",
    "start": "turbo run build && (bun run --cwd apps/api start & PORT=5174 bun run --cwd apps/web start & bun scripts/local-proxy.ts & wait)",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage && mkdir -p coverage && bun scripts/normalize-lcov.ts $(find apps packages -name 'lcov.info' -path '*/coverage/*') && lcov-result-merger '{apps,packages}/*/coverage/**/lcov.info' coverage/lcov.info",
    "test:watch:web": "bun run --cwd apps/web test:watch",
    "test:watch:api": "bun run --cwd apps/api test:watch",
    "test:watch:shared": "bun run --cwd packages/shared test:watch",
    "test:integration": "turbo run test:integration",
    "test:e2e": "turbo run test:e2e",
    "test:e2e:headed": "turbo run test:e2e:headed",
    "test:e2e:ui": "turbo run test:e2e:ui",
    "test:e2e:debug": "turbo run test:e2e:debug",
    "check": "bun run build && bun run lint && bun run test && bun run test:integration && bun run knip",
    "knip": "knip",
    "postinstall": "test -n \"$CI\" || test -n \"$DOCKER_CONTAINER\" || env | grep -q ^DEPENDABOT || lefthook install"
  },
  "dependencies": {
    "@tanstack/react-pacer": "0.23.0"
  },
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "@commitlint/types": "21.2.0",
    "@tanstack/devtools-vite": "0.8.5",
    "@tanstack/react-devtools": "0.10.12",
    "@tanstack/react-pacer-devtools": "0.14.0",
    "@tanstack/react-query-devtools": "5.102.2",
    "@techstark/opencv-js": "5.0.0-release.1",
    "knip": "6.32.2",
    "lcov-result-merger": "6.0.0",
    "lefthook": "2.1.10",
    "onnxruntime-node": "1.27.0",
    "oxfmt": "0.65.0",
    "oxlint": "1.80.0",
    "sharp": "0.35.3",
    "syncpack": "15.3.3",
    "turbo": "2.10.11"
  },
  "overrides": {
    "@hono/node-server": "1.19.14",
    "@tanstack/query-core": "5.102.2",
    "hono": "4.13.4",
    "html2canvas": "npm:html2canvas-pro@2.4.0",
    "kysely": "0.29.5",
    "rolldown": "1.2.4"
  },
  "packageManager": "bun@1.3.11",
  "//overrides": {
    "all": "Patch vulnerable transitive deps. Run `bun audit` after removing any to check if still needed. These notes sit outside `overrides` because bun records that block verbatim into bun.lock: bun >= 1.4 strips `//` keys when reading package.json but not when reading the lockfile, so a comment key inside `overrides` fails --frozen-lockfile in the Docker build.",
    "@tanstack/query-core": "Dedupe: query-db-collection and router-ssr-query peer-resolve an older query-core; TS treats identical-but-different-path type copies as distinct, collapsing createCollection generics to Record<string, unknown>.",
    "hono, @hono/node-server, lodash": "better-auth → prisma → @prisma/dev pins old versions",
    "html2canvas": "Redirect jspdf's optional html2canvas dep to html2canvas-pro (the version we already use directly). Keep this pin in step with html2canvas-pro in apps/web: dependabot does not treat an npm: alias as the same package, so a mismatch silently ships both versions as separate chunks. Enforced by scripts/check-override-pins.ts.",
    "rolldown": "Hold at the last release whose codegen is correct. rolldown 1.2.5 and 1.2.6 emit `ssr_exports` in the SSR chunk export list without ever declaring it, so .output/server/_ssr/ssr.mjs fails to link and every SSR request 500s. `vite build` and tsgo both pass, so only running the server catches it. vite 8.2.2 asks for ~1.2.4; drop this pin once a release above 1.2.6 builds a bundle where `ssr_exports` does not appear undeclared.",
    "kysely": "Dedupe: better-auth pins kysely 0.29.2 while the api uses 0.29.5; two kysely copies fork @better-auth/core into two identical-but-different-path type copies, breaking the api build."
  },
  "trustedDependencies": [
    "@swc/core",
    "@tailwindcss/oxide",
    "esbuild",
    "msw",
    "oxfmt",
    "oxlint"
  ]
}
